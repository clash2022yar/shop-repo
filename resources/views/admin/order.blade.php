@extends('layouts.admin')
@section('admin-content')<div class="page-head"><div><h1>جزئیات سفارش</h1><p>مشتری: {{ $order->user->name }} · {{ $order->user->email }}</p></div><a href="/admin/orders" class="btn btn-outline">بازگشت</a></div>@include('partials.order-detail')<section class="panel"><div class="panel-head"><h2>تغییر وضعیت سفارش</h2></div><form action="/admin/orders/{{ $order->id }}" method="post" data-method="PUT" data-ajax class="panel-body"><div class="form-grid"><label class="field"><span>وضعیت بعدی</span><select name="status">@php($next=['processing'=>['shipped'=>'ارسال شده','cancelled'=>'لغو سفارش'],'shipped'=>['delivered'=>'تحویل شده'],'delivered'=>[],'cancelled'=>[]])<option value="{{ $order->status }}">{{ $order->status_label }}</option>@foreach($next[$order->status] as $k=>$v)<option value="{{ $k }}">{{ $v }}</option>@endforeach</select></label></div><p class="muted small mt-4">لغو سفارش تنها در مرحله پردازش مجاز است و موجودی کالا را بازمی‌گرداند.</p><div class="form-errors"></div><button class="btn btn-primary mt-5">ذخیره وضعیت</button></form></section>@endsection
