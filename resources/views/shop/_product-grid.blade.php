<div class="products-grid">@forelse($products as $product)@include('shop._product-card')@empty<div class="empty-state" style="grid-column:1/-1"><x-icon name="search"/><h3>محصولی پیدا نشد</h3><p>فیلترها یا عبارت جستجو را تغییر دهید.</p></div>@endforelse</div>
@if(method_exists($products,'links') && $products->hasPages())<div class="pagination">@if($products->onFirstPage())<span>‹</span>@else<a href="{{ $products->previousPageUrl() }}">‹</a>@endif @foreach(range(max(1,$products->currentPage()-2),min($products->lastPage(),$products->currentPage()+2)) as $p)<a class="{{ $p===$products->currentPage()?'active':'' }}" href="{{ $products->url($p) }}">{{ $p }}</a>@endforeach @if($products->hasMorePages())<a href="{{ $products->nextPageUrl() }}">›</a>@endif</div>@endif