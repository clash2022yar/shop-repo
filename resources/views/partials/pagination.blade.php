@if($paginator->hasPages())<nav class="pagination" aria-label="صفحه‌بندی">@if($paginator->onFirstPage())<span class="disabled">@include('partials.icon',['name'=>'right'])</span>@else<a href="{{ $paginator->previousPageUrl() }}" rel="prev" aria-label="صفحه قبل">@include('partials.icon',['name'=>'right'])</a>@endif @foreach($elements as $element) @if(is_string($element))<span>{{ $element }}</span>@else @foreach($element as $page=>$url) @if($page==$paginator->currentPage())<span class="current" aria-current="page">@money($page)</span>@else<a href="{{ $url }}">@money($page)</a>@endif @endforeach @endif @endforeach @if($paginator->hasMorePages())<a href="{{ $paginator->nextPageUrl() }}" rel="next" aria-label="صفحه بعد">@include('partials.icon',['name'=>'left'])</a>@else<span class="disabled">@include('partials.icon',['name'=>'left'])</span>@endif</nav>@endif
