@if(count($cart['items']))<div class="cart-layout"><div class="cart-items-panel"><div class="panel"><div class="cart-list-head"><strong>کالاهای سبد خرید شما</strong><span>@money($cart['count']) کالا</span></div>@foreach($cart['items'] as $item)@php($p=$item['product'])<div class="cart-item"><img src="{{ $p->image }}" alt="{{ $p->name }}"><div class="cart-item-info"><h3><a href="/product/{{ $p->slug }}">{{ $p->name }}</a></h3><p>رنگ: {{ $p->color }}</p><p>فروشنده: دیجینو</p><small>{{ $p->stock >= $item['quantity'] ? 'موجود و آماده ثبت سفارش':'موجودی کافی نیست؛ تعداد را کاهش دهید' }}</small></div><div class="cart-item-end"><strong class="line-price">@money($item['total']) <small>تومان</small></strong><div class="quantity-actions"><div class="quantity-control"><button data-cart-quantity="{{ $p->id }}" data-quantity="{{ $item['quantity']+1 }}" aria-label="افزایش تعداد" {{ $item['quantity'] >= $p->stock || $item['quantity'] >= 99?'disabled':'' }}>@include('partials.icon',['name'=>'plus'])</button><span>@money($item['quantity'])</span><button data-cart-quantity="{{ $p->id }}" data-quantity="{{ max(0,$item['quantity']-1) }}" aria-label="کاهش تعداد">@include('partials.icon',['name'=>'minus'])</button></div><button class="icon-button" data-cart-quantity="{{ $p->id }}" data-quantity="0" aria-label="حذف کالا">@include('partials.icon',['name'=>'trash'])</button></div></div></div>@endforeach</div><a href="/products" class="btn btn-outline continue-shopping">ادامه خرید @include('partials.icon',['name'=>'left'])</a><form action="/cart/coupon" method="post" data-ajax class="coupon-form"><input name="code" placeholder="کد تخفیف دارید؟" aria-label="کد تخفیف" required maxlength="50"><button class="btn btn-outline">اعمال کد تخفیف</button><div class="form-errors w-full"></div></form>@if($cart['coupon'])<p class="small text-brand mt-2">کد {{ $cart['coupon']->code }} روی سبد خرید اعمال شده است.</p>@endif</div>@include('partials.order-summary',['checkout'=>false])</div>@else<div class="empty-state">@include('partials.icon',['name'=>'cart'])<h3>سبد خرید شما هنوز خالی است</h3><p>از میان محصولات دیجینو، همراه دیجیتال بعدی‌تان را پیدا کنید.</p><a class="btn btn-primary" href="/products">شروع خرید @include('partials.icon',['name'=>'arrow'])</a></div>@endif
