@if($orders->count())<div class="table-wrap"><table class="data-table"><thead><tr><th>سفارش</th><th>مبلغ کل</th><th>تاریخ</th><th>وضعیت</th><th></th></tr></thead><tbody>@foreach($orders as $order)<tr><td><div class="table-product">@if($order->items->first())<img src="{{ $order->items->first()->image }}" alt="">@endif<div><strong>{{ $order->number }}</strong><small>@money($order->items->sum('quantity')) کالا</small></div></div></td><td>@money($order->total) <small>تومان</small></td><td>@datefa($order->created_at)</td><td><span class="status-badge status-{{ $order->status }}">{{ $order->status_label }}</span></td><td><a href="/account/orders/{{ $order->id }}" class="icon-button" aria-label="جزئیات سفارش">@include('partials.icon',['name'=>'left'])</a></td></tr>@endforeach</tbody></table></div>@else<div class="panel-body"><div class="empty-state">@include('partials.icon',['name'=>'box'])<h3>هنوز سفارشی ثبت نکرده‌اید</h3><p>سفارش‌های شما پس از ثبت در این قسمت نمایش داده می‌شوند.</p><a href="/products" class="btn btn-primary">مشاهده محصولات</a></div></div>@endif
