@if($orders->count())<div class="table-wrap"><table class="data-table"><thead><tr><th>شماره سفارش</th><th>مشتری</th><th>مبلغ</th><th>تاریخ ثبت</th><th>وضعیت</th><th></th></tr></thead><tbody>@foreach($orders as $o)<tr><td>{{ $o->number }}</td><td>{{ $o->user->name }}</td><td>@money($o->total) تومان</td><td>@datefa($o->created_at)</td><td><span class="status-badge status-{{ $o->status }}">{{ $o->status_label }}</span></td><td><a href="/admin/orders/{{ $o->id }}" class="icon-button" aria-label="جزئیات سفارش">@include('partials.icon',['name'=>'left'])</a></td></tr>@endforeach</tbody></table></div>@else<div class="panel-body"><div class="empty-state">@include('partials.icon',['name'=>'cart'])<h3>سفارشی برای نمایش وجود ندارد</h3><p>سفارش‌های جدید پس از ثبت توسط مشتری اینجا نمایش داده می‌شوند.</p></div></div>@endif
